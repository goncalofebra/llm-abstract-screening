# Copy this file to `.env` (in this same pipeline/ folder) and fill in the keys
# for the providers you intend to use. Do NOT commit the real .env.
#
# The local model (Ollama / Qwen3) needs NO key.

OPENAI_API_KEY=
DEEPSEEK_API_KEY=
GROQ_API_KEY=

# Optional - speeds up PubMed extraction (not required):
NCBI_API_KEY=
NCBI_EMAIL=
